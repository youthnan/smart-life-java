# 服务
HOST=127.0.0.1
PORT=9000

# LLM（可选；不填也能用规则兜底）
# OpenAI 兼容接口示例（DeepSeek/通义/OneAPI 等同理）
LLM_API_KEY=""
LLM_BASE_URL=https://openrouter.ai/api/v1
LLM_MODEL=qwen/qwen3.5-flash-02-23

# Spring Boot 不会自动读本文件；在本机启动 Java 前执行：set -a && source seckill-agent/.env && set +a（或 IDE 里配置同名环境变量）

